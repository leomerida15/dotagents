/**
 * Defines the types of actions that can be performed during synchronization.
 */
export enum ActionType {
    COPY = 'COPY',
    MOVE = 'MOVE',
    DELETE = 'DELETE',
    WRITE = 'WRITE',
    MKDIR = 'MKDIR'
}
